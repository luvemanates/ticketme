<%= form_with(model: ticket_comment) do |form| %>
  <% if ticket_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket_comment.errors.count, "error") %> prohibited this ticket_comment from being saved:</h2>

      <ul>
        <% ticket_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :ticket_id, :value => @ticket_id %>
  </div>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_field :comment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
